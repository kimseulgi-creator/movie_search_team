name: Review Assign

on:
  pull_request:
    types: [opened, ready_for_review]

jobs:
  assign: 
    runs-on: ubuntu-latest
    steps:
      - uses: hkusu/review-assign-action@v1
        with:
          assignees: ${{ ji1e }} # assign pull request author
          reviewers: kimseulgi-creator, DaeHyeon Jeon, qaws7791, ZINY020, ji1e, goatisgoat
